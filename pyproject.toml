[project]
name = "nhl-game-downloader"
version = "0.1.0"
description = "Download NHL game data from public API endpoints"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
